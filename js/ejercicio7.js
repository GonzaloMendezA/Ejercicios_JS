// Ejercicio de logica
let day = prompt(" Ingrese dia");

if (day === null || day === ("")) {
  alert(day + "opcion invalida");
} else {
  day = day.toLowerCase();
if (day = day)toLowerCase() === "" || day === null
  alert(day + "opción invalida");
if (
  day === "lunes" ||
  day === "martes" ||
  day === "miercoles" ||
  day === "miércoles" ||
  day === "jueves" ||
  day === "viernes"
) {
  alert("Es un dia entre semana");
} else {
  //if ANIDADOS
  if (day === " sabado" || day === " sábado" || day === " domingo") {
  } else {
  }
  alert("Favor ingresar solo dias de la semana");
}
}
/**
 * NOTAS
 * 1- Mayusculas
 * 2 _ palabras que no son dias
 * 3 - vacio
 * 4 - no le da nada
 * 5
 */
